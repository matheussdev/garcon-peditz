// TUDO ISSO AQUI TEM  QUE SER VARIÁVEL

export enum ColumnType {
  PENDING = "Pendente",
  PRAPARING = "Preparando",
  DONE = "Pronto",
  DELIVERED = "Saiu para Entrega",
}

export enum ItemType {
  ORDER = "Order",
}
